fastapi
uvicorn[standard]
pydantic-settings
python-dotenv
httpx

# RAG pipeline
langchain
langgraph
qdrant-client
sentence-transformers       # reranker ONLY (CrossEncoder) — NOT used for embedding
rank-bm25                   # BM25 sparse retrieval

# Document parsing
unstructured[pdf,docx]
pymupdf

# LLM + Embedding clients (one package covers both)
openai                      # text-embedding-3-small for embed, gpt-4o-mini for LLM

# Evaluation
ragas

# Cache & DB
redis
aiofiles